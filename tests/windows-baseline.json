{
  "failures": 56,
  "errors": 3,
  "measured_on": "claude/issue-99-75416a a0c67a1, windows-latest 10.0.26100, python 3.13",
  "note": "All 3 errors are KeyError: 'HOME' from tilde-expansion tests (Q40). The failures are POSIX-assuming fixtures and parser bugs (Q39). Raised 54 -> 56 for test_glob_item_after_cd_outside_deny and test_glob_item_escaping_body_path_flagged, which use a POSIX absolute path that is drive-relative on Windows; that is Q39's root cause, not a behavior change. Tighten as those land; never raise to get green."
}
